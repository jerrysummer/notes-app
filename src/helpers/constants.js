const ADD_NOTE = 'add_note';
const EDIT_NOTE = 'edit_note';
const DELETE_NOTE = 'delete_note';

const noteColors = ['red', 'yellow', 'green', 'blue'];

export {
  ADD_NOTE,
  EDIT_NOTE,
  DELETE_NOTE,
  noteColors,
}